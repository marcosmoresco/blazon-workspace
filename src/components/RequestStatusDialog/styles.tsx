import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
  center: {
    textAlign: 'center'
  },
  title: {
    fontWeight: 600,
    textAlign: 'center',
    fontSize: 24,
    color: '#26213F'
  },
  subtitle: {
    textAlign: 'center',
    fontSize: 16,
    color: '#A8A6B2'
  }
})
